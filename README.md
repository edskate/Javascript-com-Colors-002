# Javascript-com-Colors-002
```
Document.getElementById()
O Documentmétodo getElementById()retorna um Elementobjeto que representa o elemento cuja idpropriedade 
corresponde à string especificada. Como os IDs de elemento devem ser exclusivos se especificados,
eles são uma maneira útil de obter acesso a um elemento específico rapidamente.
```

# Parâmetros
## id
```
O ID do elemento a ser localizado. A ID é uma string que diferencia maiúsculas de minúsculas
e é exclusiva no documento; apenas um elemento pode ter um determinado ID.
```
